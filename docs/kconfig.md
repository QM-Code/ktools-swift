# `kconfig` In `ktools-swift`

`ktools-swift/` does not currently include a checked-in `kconfig`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `kconfig` is not yet implemented in the current Swift workspace
- this markdown file is the canonical source for that status in the website
