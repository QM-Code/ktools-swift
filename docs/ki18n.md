# `ki18n` In `ktools-swift`

`ktools-swift/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` is not yet implemented in the current Swift workspace
- future Swift implementation docs should replace this file in-place
