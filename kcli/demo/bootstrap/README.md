# Bootstrap Demo

Import and integration smoke test for the Swift `Kcli` package.

Run from [`demo/`](../README.md):

```bash
swift run --scratch-path ../build/latest/swiftpm-demo kcli-demo-bootstrap
```
